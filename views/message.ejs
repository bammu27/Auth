<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Registration Status</title>
    <!-- Include Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5">
        <div class="text-center">
            <% if (route === 'login') { %> 
                <h1 class="display-4 text-primary">You are  already registered</h1>
                <p class="lead">Go ahead and log in.</p>
            <% } else { %>
                <h1 class="display-4 text-primary">You are not registered</h1>
                <p class="lead">Go ahead and signup to create account</p>
            <% } %>
            <form action='<%= route %>' method="GET">
                <button class="btn btn-primary btn-lg" type="submit"><%= route === 'login' ? 'Log In' : 'create account' %></button>
            </form>
        </div>
    </div>

    <!-- Include Bootstrap JS and Popper.js (if needed) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/popper.js@2.11.6/dist/umd/popper.min.js"></script>
</body>
</html>
